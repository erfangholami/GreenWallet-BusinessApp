<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/margin_small"
    tools:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_category_icon"
        android:layout_width="@dimen/view_category_default"
        android:layout_height="@dimen/view_category_default"
        android:layout_marginTop="@dimen/margin_medium"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/category_accessories_icon" />

    <TextView
        android:id="@+id/tv_category_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_small"
        android:layout_marginBottom="@dimen/margin_medium"
        android:fontFamily="@font/maison_neue_demi"
        android:textAlignment="textStart"
        android:textColor="@color/colorText"
        android:textSize="@dimen/text_small_xx"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/iv_category_icon"
        app:layout_constraintStart_toStartOf="@+id/iv_category_icon"
        app:layout_constraintTop_toBottomOf="@+id/iv_category_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>